<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ift604.bingo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <service
            android:name=".service.GetLobbyByAttributeService"
            android:exported="false"></service>
        <service
            android:name=".service.JoinLobbyService"
            android:exported="false" />

        <activity android:name=".WaitLobbyActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.PlayerCardService"
            android:enabled="true" />
        <service
            android:name=".service.FindLobbyNearMeService"
            android:enabled="true" />
        <service
            android:name=".service.CreateLobbyService"
            android:enabled="true" />
        <service
            android:name=".service.CreateUserService"
            android:enabled="true" />
    </application>

</manifest>